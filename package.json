{
  "name": "drive-sorter",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "node scripts/clean-build.mjs && tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "verify": "npm run typecheck && npm run build && node scripts/verify.mjs",
    "push": "npm run build && clasp push",
    "pull": "clasp pull",
    "open": "clasp open-script",
    "status": "npm run build && clasp status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.12",
    "typescript": "^7.0.2"
  }
}
